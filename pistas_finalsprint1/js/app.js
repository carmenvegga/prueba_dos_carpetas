var testArray = [];
for (var i = 0; i <= 10; i ++){
//agregar un nuevo elemento al final del array
testArray.push(i);
}
console.log(testArray);

//eliminar el primer elemento del arreglo
  testArray.shift();
console.log(testArray);

//eliminar el último elemento del array
  testArray.pop();
console.log(testArray);

//invertir los elementos del arreglo
  testArray.reverse();
console.log(testArray);

var testString = "abcde"
 testString.charAt(2); */
/*Para obtener un caracter a partir de su código Ascii, utilizo el método
string.fromCharcode */
/* var codigo = testString.charCodeAt(2);
console.log(codigo);
*/
